{
  "name": "{{name}}",
  "description": "{{description}}",
  "private": true,
  "dependencies": {
{{#each dependencies}}
  "{{artifactId}}": "{{version}}",
{{/each}}
  },
  "scripts": {
    "start": "./node_modules/.bin/vertx run server.js"
  }
}
